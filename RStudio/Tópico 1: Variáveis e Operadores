#############################################
# Tópico 1: Variáveis e Operadores no R
# Desenvolvido por Thamara Crispim
#############################################

### VARIÁVEIS ###

# Em R, criamos variáveis usando o operador de atribuição "<-"
# Variáveis servem para armazenar diferentes tipos de dados

# Variável numérica
nota <- 8.5

# Variável inteira (precisa da função as.integer)
idade <- as.integer(25)

# Variável de texto (caractere)
nome <- "Thamara"

# Variável lógica (booleana)
aprovada <- TRUE

# Variável fator (útil para dados categóricos)
sexo <- factor("Feminino")

# Verificando as classes das variáveis
class(nota)      # numeric
class(idade)     # integer
class(nome)      # character
class(aprovada)  # logical
class(sexo)      # factor


### TIPOS DE VARIÁVEIS (CONCEITOS) ###

# Variáveis Quantitativas: representam números, podem ser medidas
altura <- 1.65   # exemplo de variável quantitativa

# Variáveis Qualitativas: representam categorias, grupos
cor_olhos <- factor("Castanho")  # exemplo de variável qualitativa


### OPERADORES ARITMÉTICOS ###

# Soma
3 + 2    # 5

# Subtração
5 - 1    # 4

# Multiplicação
4 * 2    # 8

# Divisão
10 / 2   # 5

# Potência
2 ^ 3    # 8

# Resto da divisão
10 %% 3  # 1

# Raiz quadrada
sqrt(49) # 7

# Logaritmo
log(10)             # logaritmo natural
log(10, base = 10)  # logaritmo de base 10


### OPERADORES LÓGICOS ###

# Igual a
5 == 5   # TRUE

# Diferente de
5 != 3   # TRUE

# Menor ou igual
4 <= 4   # TRUE

# E lógico (ambos precisam ser verdadeiros)
TRUE & FALSE  # FALSE

# OU lógico (pelo menos um precisa ser verdadeiro)
TRUE | FALSE  # TRUE

# Negação (NÃO)
!TRUE  # FALSE


### SEQUÊNCIAS ###

# seq(): cria uma sequência numérica
seq(1, 10, by = 2)  # 1, 3, 5, 7, 9

# rep(): repete valores
rep(1:3, times = 2)   # 1 2 3 1 2 3
rep("R", times = 5)   # "R" "R" "R" "R" "R"

### BOAS PRÁTICAS ###

# Sempre nomeie variáveis de forma clara e sem acentos
# Evite usar caracteres especiais (ex: ç, é, ã)
# Use class() ou str() para entender seus objetos
# Execute uma linha com Ctrl + Enter no RStudio
# Execute o script inteiro com Ctrl + Shift + Enter

